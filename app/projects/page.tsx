import ProjectsContainer from '@/containers/ProjectsContainer'
import React from 'react'

export const metadata = {
    title: 'Our Projects | iconic PEB Structures',
    description: 'Browse our portfolio of completed and ongoing pre-engineered building projects across industrial, commercial, and specialized sectors.',
}

const page = () => {
    return (
        <ProjectsContainer />
    )
}

export default page